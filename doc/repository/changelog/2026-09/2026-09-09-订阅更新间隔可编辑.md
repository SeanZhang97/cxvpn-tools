# 订阅更新间隔可编辑

- 日期：2026-09-09
- 功能：订阅编辑表单
- 分支：main

## 问题根因

新增订阅默认关闭自动更新，表单同时将“自动更新间隔”输入框设为 disabled，导致用户无法在开启前预先配置间隔。

## 修复内容

- 自动更新间隔仅在表单忙碌提交时禁用。
- 关闭自动更新时仍可编辑并保存分钟数，开启后直接使用已配置值。

## 涉及文件

- `ui/routing.js`

## 验证情况

- `node tests/test_ui_routing.js`
- `node tests/test_ui_polish.js`
