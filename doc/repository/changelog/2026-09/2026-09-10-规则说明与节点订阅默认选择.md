# 2026-09-10 规则说明与节点订阅默认选择

- 日期：2026-09-10
- 功能：规则页辅助说明与节点订阅默认选择
- 分支：main
- 需求概述：优化规则页规则组成说明的视觉层级；进入节点页时优先定位当前正在使用的代理订阅。
- 涉及文件：`ui/routing_rules.css`、`ui/routing.js`、`tests/test_ui_polish.js`、`tests/test_ui_routing.js`
- 关键设计：规则组成说明使用较小字号、行距和分隔线，长文本允许换行；节点页按 `default_outbound` 指向的订阅、手动节点偏好订阅、首个启用订阅的顺序选择默认分组。
- 影响范围：仅影响规则页展示和节点页进入时的默认分组，不改变规则生成、节点选择保存或代理运行配置。
- 验证：`node tests/test_ui_polish.js`、`node tests/test_ui_routing.js`、`node tests/test_ui_routing_nodes.js`、`git diff --check`；已完成 PyInstaller 打包并启动 `dist/CXVPNTools/CXVPNTools.exe`。
- 注意事项：用户在节点页主动选择其他订阅后仍可继续操作；再次从导航进入节点页会重新按当前运行配置定位。
