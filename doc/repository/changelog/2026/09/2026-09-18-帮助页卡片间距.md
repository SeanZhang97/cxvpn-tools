# 帮助页卡片间距

- 日期：2026-09-18
- 分支：`main`
- 需求与根因：帮助页“软件更新”与“四步完成基础配置”卡片缺少外间距，边框紧贴。
- 涉及文件：`ui/style.css`。
- 关键设计：仅为帮助页中紧邻设置卡片的快速入门卡片增加 `14px` 顶部外间距，与下方帮助模块间距一致。
- 影响范围：帮助页卡片排版，不改变内容、交互或其它页面。
- 验证情况：`node tests/test_ui_polish.js`、帮助页下拉框静态核对及 `git diff --check` 通过；`uv run --with pyinstaller python build.py` 成功，主库配置（revision 147）及兼容 JSON 摘要核对通过，新版进程已启动。产物样式与源码一致，未发现用户配置、主库或凭据文件。未执行界面视觉回归。
- 知识沉淀：本次未发现需要新增或修正的稳定可复用知识。
- 注意事项：需使用重新打包后的程序加载新样式。
