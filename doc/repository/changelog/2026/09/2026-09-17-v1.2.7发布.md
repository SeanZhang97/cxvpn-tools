# v1.2.7 发布

- 日期：2026-09-17
- 功能：发布节点测速范围与单节点测速版本
- 分支：`main`

## 发布内容

- 节点页支持按当前可见范围执行批量测速，避免筛选后仍对无关节点发起测速。
- 支持直接对单个节点测速，并复用现有任务互斥、版本保护和结果持久化流程。
- 调整测速任务与界面状态同步，确保旧任务结果不会覆盖较新的筛选或测速状态。
- 优化节点测速相关布局、交互状态和说明文档。

## 验证与产物

- Python 语法检查通过。
- 路由离线回归 296 项全部通过。
- 发行构建离线回归 13 项、安装器锁定清单 16 项、前置依赖执行器 7 项及路由 UI 回归通过。
- 使用 `build_release.py` 生成受保护轻量安装器、便携 ZIP、SHA-256 文件和 manifest；具体大小与摘要以 Release manifest 为准。
- Git tag：`v1.2.7`。

## 注意事项

- 未使用 Authenticode 产品证书签名时，Windows 可能显示未知发布者提示。
- 用户数据继续使用 `%LOCALAPPDATA%\CXVPNTools` 中的 SQLite 主库；发行产物不得包含配置、凭据、日志或恢复备份。
- 本轮不执行真实首次安装、覆盖升级、卸载或系统重启恢复测试。
